Los notebooks de exploración viven junto al dataset privado (no versionado).

Para prototipos ligeros, crear aquí `*.ipynb` que importen:

```python
from consejos_guerra.paths import resolve_data_root
from consejos_guerra.characterize.scan import characterize_dataset
```

Los notebooks anteriores de cruce de RUTs están en `archive/dataframe-crossing/`.
